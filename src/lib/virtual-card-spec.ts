/** Pixel size for uploaded virtual card artwork (ISO ID-1 width:height ratio). */
export const VIRTUAL_CARD_DESIGN_WIDTH = 1200;
export const VIRTUAL_CARD_DESIGN_HEIGHT = 756;

export const VIRTUAL_CARD_SPEC_LABEL = `${VIRTUAL_CARD_DESIGN_WIDTH} × ${VIRTUAL_CARD_DESIGN_HEIGHT} px`;

export const VIRTUAL_CARD_BUCKET = "vendor-virtual-cards" as const;
